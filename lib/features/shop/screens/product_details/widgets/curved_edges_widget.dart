import 'package:flutter/material.dart';

class TCuredEdgesWidget extends StatelessWidget {
  const TCuredEdgesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return ClipPath();
  }
}
